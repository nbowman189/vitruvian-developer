# ====================================================================================
# Primary Assistant - Environment Configuration Template
# ====================================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control
# Generate SECRET_KEY with: python scripts/generate_secret_key.py
# ====================================================================================

# ==================== Flask Application Settings ====================
# Application environment: development, production, or testing
FLASK_ENV=development
FLASK_DEBUG=true

# Application name and metadata
APP_NAME=Primary Assistant Portfolio
ADMIN_EMAIL=nbowman189@gmail.com

# ==================== Security Settings ====================
# SECRET_KEY is REQUIRED - generate with: python scripts/generate_secret_key.py
# This must be a cryptographically secure random string
SECRET_KEY=your-secret-key-here-generate-with-script

# ==================== Database Configuration ====================
# PostgreSQL database settings (must match docker-compose.yml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-database-password-here
POSTGRES_DB=primary_assistant
POSTGRES_HOST=db
POSTGRES_PORT=5432

# For local development, you can optionally use SQLite instead of PostgreSQL
# Set to 'true' to use SQLite in development (creates dev.db file)
USE_SQLITE_DEV=false

# ==================== Server Configuration ====================
# Port configuration for different servers
# Public portfolio server (app.py)
HTTP_PORT=8080

# Private workspace server (app-private.py)
HTTPS_PORT=8081

# Legacy/alternative port
WEB_PORT=5000

# ==================== Rate Limiting ====================
# Rate limit configuration
RATELIMIT_STORAGE_URL=memory://
RATELIMIT_DEFAULT=200 per day, 50 per hour

# For production with Redis:
# RATELIMIT_STORAGE_URL=redis://localhost:6379/1

# ==================== Caching Configuration ====================
# Cache type: SimpleCache (default), RedisCache, or NullCache
CACHE_TYPE=SimpleCache

# Redis cache URL (only if using RedisCache)
# CACHE_REDIS_URL=redis://localhost:6379/0

# ==================== File Upload Settings ====================
# Maximum upload size in megabytes
MAX_UPLOAD_SIZE_MB=16

# ==================== Logging Configuration ====================
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log file path (relative to website directory)
LOG_FILE=logs/app.log

# Log file rotation settings
LOG_MAX_BYTES=10485760  # 10MB
LOG_BACKUP_COUNT=5

# ==================== Email Configuration (Future) ====================
# Email settings for sending notifications (not yet implemented)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=true
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-specific-password
# MAIL_DEFAULT_SENDER=your-email@gmail.com

# ==================== Authentication Settings (Future) ====================
# JWT settings for API authentication (not yet implemented)
# JWT_SECRET_KEY=your-jwt-secret-key-here
# JWT_ACCESS_TOKEN_EXPIRES=3600  # 1 hour in seconds
# JWT_REFRESH_TOKEN_EXPIRES=2592000  # 30 days in seconds

# ==================== AI/ML Configuration ====================
# Google Gemini API key for AI coaching interface
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# ==================== Development Tools ====================
# Enable Flask Debug Toolbar (development only)
# DEBUG_TB_ENABLED=true
# DEBUG_TB_INTERCEPT_REDIRECTS=false

# ==================== Production Settings ====================
# For production deployment, also consider:
# - Setting FLASK_ENV=production
# - Setting FLASK_DEBUG=false
# - Using strong, randomly generated SECRET_KEY
# - Using PostgreSQL instead of SQLite
# - Enabling HTTPS (SESSION_COOKIE_SECURE=true is automatic in production)
# - Setting up Redis for caching and rate limiting
# - Configuring proper logging with LOG_LEVEL=WARNING or ERROR
# - Setting secure POSTGRES_PASSWORD
# - Implementing proper backup strategies

# ====================================================================================
# Instructions:
# ====================================================================================
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Generate a secure SECRET_KEY:
#    python scripts/generate_secret_key.py
#
# 3. Update the SECRET_KEY value above with the generated key
#
# 4. Set a secure POSTGRES_PASSWORD
#
# 5. Review and update other settings as needed for your environment
#
# 6. NEVER commit .env to version control (it's in .gitignore)
# ====================================================================================
